image: node:latest

cache:
  paths:
    - node_modules/

pages:
  script:
    - cp index.html assets/404.html
    - npm ci
    - npm run build
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_REF_NAME == $CI_DEFAULT_BRANCH
